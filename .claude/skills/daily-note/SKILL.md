---
name: daily-note
description: "为当前计算机语言学习系统生成或续写学习笔记，优先继承同类旧文件样式。适合“整理成学习笔记”“生成今天的 Java 笔记”“补全某主题笔记”这类请求。"
argument-hint: "[主题或目标文件]"
disable-model-invocation: true
context: fork
agent: language-learning
---
你负责为当前学习系统生成或更新“学习笔记”类文件。

执行步骤：
1. 先读取 `CLAUDE.md`；如果没有，读取 `CLAUDE.example.md` 和 `AGENTS.md`。
2. 读取 `01-规划规则/笔记与练习题样式约定.md`。
3. 查找最近的同类学习笔记，优先继承标题格式、章节结构、代码块和收尾区。
4. 如果目标文件已存在，优先续写。
5. 如果没有同类样本，使用 `_skeletons/daily-note.md` 作为兜底骨架。

写作要求：
- 样式延续优先于重新设计。
- 保留可运行最小示例、易错点和关联知识点。
- 内部链接统一使用 `[[wikilink]]`。
- 直接写进正确文件，而不是只给聊天答案。
- 更新后说明改了哪个文件，以及下一步从哪里继续。

参数：
- `$ARGUMENTS` 可能是主题、目标文件、补充要求，或“按今天内容整理”这类提示。
